//a class to handle client work
class Client{
    constructor(){

    }
    //a method to send json data entred in text area to the server as a post
    submit(){  

        
        // var post = $.post('http://localhost:3000/json',{jsonData:"jquery here",dataType:'jsonp'})
        // post.done(function(data){
        //     var content = $(data).find("#textArea");
        //    console.log(content)
        // })
    }
}
var aClient = new Client();


